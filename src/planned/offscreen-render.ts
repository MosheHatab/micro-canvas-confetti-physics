/**
 * Planned v1+ API — OffscreenCanvas rendering in a worker.
 * Not exported from the package. Calling throws until implemented.
 */
export function createOffscreenRenderer(_canvas: HTMLCanvasElement): never {
	throw new Error(
		"[micro-canvas-confetti] OffscreenCanvas rendering is planned for v1+ and is not implemented yet. See ROADMAP.md.",
	);
}
